import { Component } from '@angular/core'

@Component({
    selector: 'app-header', //define qué etiqueta de HTML lo carga
    templateUrl: './header.component.html' //defino qué HTML se utiliza para renderizar la etiqueta
})
export class HeaderComponent{
    // esto mapea HTML a componentes angular

}